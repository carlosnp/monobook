import { ButonsModule } from './butons.module';

describe('ButonsModule', () => {
  const module: ButonsModule = new ButonsModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
