import { EmptyCardModule } from './empty-card.module';

describe('EmptyCardModule', () => {
  const module: EmptyCardModule = new EmptyCardModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
