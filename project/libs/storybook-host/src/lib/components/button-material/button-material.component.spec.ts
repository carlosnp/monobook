import { ButtonMaterialComponent } from './button-material.component';

describe('ButtonMaterialComponent', () => {
  let component: ButtonMaterialComponent;

  beforeEach(() => {
    component = new ButtonMaterialComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
